import React from "react";

import { Header, Terminal } from "../components";

export default function Index() {
    return (
        <div>
            <Header />
            <Terminal />
        </div>
    );
}
